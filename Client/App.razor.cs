namespace Client;

// Declares the App component's partial class in C# so the C# language service and analyzers
// (used by the IDE) always resolve the type, while App.razor is compiled into the other partial.
// Markup alone can leave "App" unresolved in top-level Program.cs even when dotnet build works.
public partial class App
{
}
